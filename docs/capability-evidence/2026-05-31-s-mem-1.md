# S-MEM-1 — Resident memory facade + qmd facts store

**Date:** 2026-05-31  
**Packet:** S-MEM-1  
**Verdict:** substrate verified; no live stack required for this packet

## What changed

- Added `FactsStore`, the formal owner for `data/controller/memory/<resident>/facts/<topic>.md`.
- Added `ResidentMemoryService` with `rememberFact`, `readTopic`, `relevantTo`, and a typed `searchSemantic` stub for S-MEM-2.
- Routed `MemoryStore.retrieve` through `FactsStore.relevantTo` so existing prompt-memory retrieval keeps working through the formal qmd facts path.
- Preserved the old CQA8 `facts/world-events.md` layout; no migration or file move is required.

## Evidence

- Red test first: focused memory tests initially failed because `facts-store` and `resident-memory-service` modules did not exist.
- Green focused test:
  - `npm test -- --runTestsByPath src/controller/memory/facts-store.test.ts src/controller/memory/resident-memory-service.test.ts src/controller/memory/memory-store.test.ts --runInBand --no-coverage`
  - Result: `3` suites, `13` tests passed.
- Typecheck:
  - `npm run typecheck`
  - Result: PASS.
- Full gate:
  - `npm run fin`
  - Result: PASS, `234` suites and `3327/3327` tests.
- Build:
  - `npm run build`
  - Result: PASS, `832` files compiled.
- Server UI boundary:
  - `npm run check:no-ui`
  - Result: PASS.
- Build:
  - `npm run build`
  - Result: PASS, `832` files compiled.
- Full finish gate:
  - `npm run fin`
  - Result: PASS, `234` suites and `3326/3326` tests passed.

## Proven behaviors

- Per-resident isolation: `res:hans` facts do not leak into `res:pip` reads/searches.
- Append-only timestamps: repeated `rememberFact` calls append distinct ISO-timestamped lines.
- Restart survival: a new store/service instance with the same memory root reads previously written facts.
- Proto-qmd compatibility: existing `facts/world-events.md` is readable by topic and searchable by relevance.
- Existing retrieval compatibility: `MemoryStore.retrieve` still returns `Fact memory (<topic>.md): ...` excerpts through the new formal store.

## Remaining work

- S-MEM-2: semantic memory / mem0 or approved equivalent plus prompt-envelope Memory block.
- S-MEM-3: Brain-driven `rememberFact` decisions and MCP/operator memory tools.
- S-MEM-4: retention and summarization policy.
- Live capability proof still needed for ordinary named residents remembering newly learned NPC/quest/death facts over longer delays.
