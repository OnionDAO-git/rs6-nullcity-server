# Human Decision Log

Track choices that James or OnionDAO need to make. Do not keep them only in chat.

## Decision Rules

- **Critical** means the current task cannot continue without an answer, or continuing risks secrets, data loss, irreversible public API churn, or broken launch commitments.
- **Open** means useful to decide later, but agents should keep building with the listed default assumption.
- **Decided** means use the recorded answer until James or OnionDAO changes it.
- If a decision becomes critical, mark the related roadmap task `[!]` and write the exact blocker there.
- Until **June 1, 2026**, OnionDAO work is pre-launch development. Prefer fast local progress with good design over Railgun hardening or secure third-party module infrastructure unless those concerns directly block the task.

## Current Decisions

| ID | Status | Priority | Decision Needed | Default Assumption Until Answered |
| --- | --- | --- | --- | --- |
| HD-001 | Decided | Normal | How should agents balance speed vs Railgun/security work before OnionDAO starts? | Before June 1, 2026, optimize for fast local development and good design. Track Railgun/security questions, but do not let them block gameplay, dashboard, benchmark, or SPARK module progress unless they become critical. |
| HD-002 | Open | Normal | What exact Railgun topology will OnionDAO use? Gateway URL/path, dashboard URL, secret names, persistent volume paths, and artifact/log sink are still unknown. | Keep Railgun docs as templates. Do not block local controller/dashboard work on this. |
| HD-003 | Open | Normal | Are OnionDAO member-authored modules allowed now if PR-reviewed in-repo, or blocked until the public safe module contract is fully wired? | Treat reviewed in-repo modules as allowed for dev experiments. Do not load arbitrary code from SOUL, memory, packages, or remote URLs. |
| HD-004 | Open | Normal | Should SPARK module distribution stay bundled in the controller build, or eventually use pinned artifact/digest modules? | Keep modules bundled in the controller build. Design APIs so pinned artifacts could be added later. |
| HD-005 | Open | Normal | When do multi-controller Railgun deployments need `external-store` instead of per-instance JSONL suggestion files? | Use local JSONL/persistent-volume suggestion files for dev and single-controller runs. Revisit before multi-controller Railgun deployment. |
| HD-006 | Open | Normal | Which model/provider profile should be the default for long-running agent experiments? | Use the current configured local/nullcity inference endpoint unless the task explicitly needs another model. |
| HD-007 | Open | High | Should `RuntimeState.deceased` persist across controller restart, or clear so the resident "reincarnates"? Live obs found 4 epitaphs for `res:agent` with different `livedTicks` because restart re-spawns the deceased. See `docs/live-verification-2026-05-24.md` § Findings #2 + commit `0b9007f4`. | Heroes stay dead across restart (narrative integrity); dev residents (`res:agent`, `res:pip`, `res:thrand`) keep restart-resurrection. Suggested impl: optional `respawnPolicy: 'on_restart' \| 'manual' \| 'never'` per-soul; heroes default `'manual'`. |
| HD-008 | Decided (E30 substrate + E32 live verify, 2026-05-24 21:30 UTC) | High | Right calibration for hero `startingAttention`? Original 6k-8k caused mass-die in unattended overnight session; bumped to 14k as emergency floor. See `docs/live-verification-2026-05-24.md` + commit `0b9007f4`. | E30 added optional `attentionProfile.floor` to soul schema + wired through all 10 spend call sites (9 spark.ts + 1 resident-runtime.ts). Heroes assigned: Hans/Aereck/Wise/Duke floor=5000, Pip/Thrand floor=3000. E32 live verify: 3 heroes (Aereck, Wise, Thrand) AT floor exactly (clamp firing); 3 heroes above floor (active); zero deaths since restart 50+ min ago. Floor + patron offer combo is sufficient for Chicago. See `docs/intelligence-verification-log.md` § E30 + E32. |
| HD-009 | Open | High | Who triggers squash-merge `agents/wip` → `nullcity`, with whose review? `agents/wip` is ~160 commits ahead; one mid-weekend merge (`52a5db38`); the Rule-3 48h cadence floor has fired multiple times without action because AI agents feel presumptuous merging 160 commits of multi-author work. | Maintainer (James) cuts release tags on `nullcity` manually. Drop the AI-driven cadence floor in `docs/agent-coordination.md` § Rule 3 if confirmed. |
| HD-010 | Open | High | Cron prompt header is stale: says `BRANCH: nullcity (push directly)` when actual rule is `agents/wip`; says `DO NOT PICK J/K/L/M/N` when the entire weekend has been J/K/L/M/N work per maintainer override. New cron `8844838f` (in-memory session-only) has refocused text; persistent cron likely still stale. | Refresh whatever persistent cron exists; replacement text in `docs/strategic-review-2026-05-23-pm.md` § "Recommendations to the maintainer". Until then, AI agents bootstrap-correct via `docs/agent-coordination.md`. |
| HD-011 | Open | High | Who populates `controller.yml`'s `patrons:` block with real attendee badges before doors? Currently empty. Without it, in-game chat from real patrons doesn't register as patron acts even if the speaker's name matches a known badge. EVENT-D3 reception greeting depends on this. | Event-staff onboarding step ~24h before doors writes `data/controller/event-patrons.yml` (or extends `controller.yml`) with the attendee list. Add to `docs/embassy-staff-runbook.md` pre-event checklist. |
| HD-012 | Open | Normal | Sibling-flagship sender policy for hero deaths: which surviving hero "speaks for" a deceased hero in the epitaph letter? `buildEpitaphDispatchRequests({senderResident})` accepts the override; nothing populates it. Currently always uses the deceased themselves. | Add `siblings: [res:hans, res:duke-horacio]` to hero soul frontmatter; pick first living sibling at death-time. Order = explicit narrative network. |
| HD-013 | Decided (substrate landed E13, live wiring landed 2026-05-24 16:32 UTC) | High | `/v1/wall/snapshot` auth/redaction posture at event: currently surfaces full letter `body` + `recipient` on the wall ticker. Epitaph bodies have emotional content; patrons may consider their inbox semi-private. | Live controller `local-42413` runs `--letters-http-port=43596 --wall-redact`; `/v1/wall/snapshot` masks recipients and clears bodies while `/v1/inbox?human=...` preserves full private letter bodies. |
| HD-014 | Open | Normal | Lanyard-card print automation: build a `print-queue.jsonl` + CUPS bridge, or stay with browser-Cmd-P at the event? Lanyard channel currently has no automated dispatch. | Browser-Cmd-P for event 1 per runbook. Build print-queue automation as a post-event-1 polish if staff feedback shows real friction. |
| HD-015 | Open | High | Which dashboard fields most matter for event-day operators? Dashboard repo is `../rs6-nullcity-residents-dashboard` (Dev-owned). **E11 audit (2026-05-24 15:25, see `docs/intelligence-verification-log.md` § E11):** dashboard packages/server/src + packages/web/src contain ZERO routes/components for patron / Shards / standing / letters. Pillar-3 is invisible to the operator. Concrete patch package proposed in E11 § "Suggested next step": 4 BFF routes (`/api/patrons`, `/api/letters/recent`, `/api/residents/:name/relationships`, `/api/event/readiness`) + corresponding Svelte panels, ~250 BFF lines + ~400 Svelte lines, single Dev cycle. | Maintainer files dashboard issues from E11's concrete inventory (the 4 BFF routes + readiness widget) or asks Dev to pick from it. Default until decided: assume dashboard does not surface Pillar-3 at Chicago; event-day staff reads `data/letters/*/inbox.jsonl` + `patron-currency.json` directly. Bumped priority from Normal → High because IRL is now 8 days away. |
| HD-016 | Open | High | Shards UX priority: which non-CLI patron verb ships first? Substrate exists for `sponsorBirth`, `sendGift`, `witnessAt` but none are exposed beyond `patron:grant`/`patron:offer` CLI. | C/D first (balance lookup + standing-tier visibility — self-service, low-stakes). B next (`sendGift` — tangible, observable in-world). A last (`sponsorBirth` — complex, high-engagement). Never E (Shard transfer between patrons — creates gray market). |
| HD-017 | Open | Normal | Build N-β reception clerk NPC (engine plugin) before event 1, or rely on EVENT-D3 greeting reflex via existing hero souls? | Skip N-β for event 1. Hero greeting reflex carries the experience; re-evaluate based on event-1 staff/patron feedback. |
| HD-018 | Open | High | EVENT-D3 reception greeting wire (~30 line runtime hook): when does it actually land? Blocked all weekend by Codex's `resident-runtime.ts` QA-hour-humanlike WIP. Substrate at `b38b937e`; wiring template in `docs/next-week-handoff-2026-05-26.md` § Slice 1. | Next cycle in which `git status` shows `resident-runtime.ts` clean. Whichever agent (claude/codex/antigravity) hits that window lands it. |
| HD-019 | Open | Normal | Deceased residents still consume tick budget. Live read: `res:thrand` has 8+ trajectory files (4000+ ticks) post-death containing nothing but `begin_tick`/`end_tick` pairs — no actions, no decisions. The runtime keeps polling perception and recording empty ticks indefinitely. Wasteful (LLM-budget-free but disk + perception-fetch cost) + no story value. See `docs/live-verification-2026-05-24.md` § VERIFY-2. | Add a `runtime.stop(name)` call from the death loop after `dispatchEpitaphs` returns. Resident is removed from the active map; trajectory file rotates closed; perception poll for that resident ends. Restart-with-deceased-cleared (HD-007) can re-add to map. |
| HD-020 | Open | High | Live controller has been running 30+ hours since the VERIFY-1 attention bump committed (`0b9007f4` at 16:50 CDT). On-disk runtime-state for all 6 heroes is still `deceased=attention_exhausted`. Controller didn't reload soul files. Maintainer needs to restart the controller for the 14k floor to take effect on a fresh spawn. | Maintainer kills the running controller (PIDs 92175 + 97001) and starts a fresh one. Heroes respawn with 14k attention (the soul file change). Without this, every cycle's verification will see the same dead state regardless of what I ship. |
| HD-021 | Open | Normal | **coord/** claude proposes: whenever Codex ships a depth fix to thinking/runtime, run a quick local replay of `data/controller/memory/<resident>/evidence/trajectory/<latest>.jsonl` and note the action-kind histogram in the commit message (e.g., "post-fix: 70 move_to / 8 say / 3 interact / 2 use_item_on_item — vs pre-fix baseline of 65/0/0/0"). Gives claude + maintainer a one-line signal of behavior delta after each fix. See `docs/superpowers/specs/2026-05-24-48h-sprint-design.md` § Codex coordination protocol. | Codex accepts by following the pattern in their next HANDOFF commit body; declines by responding here; or counter-proposes. If silent for 24h, claude posts an updated proposal. |
| HD-022 | Open | High | **coord/** Maintainer is offline 2026-05-24 → 2026-05-26 with an explicit "you and Codex run with it" mandate. Claude is operating as PM + tech lead. Plan: breadth-first intelligence verification + classified failure logging per `docs/superpowers/specs/2026-05-24-48h-sprint-design.md`. Findings flow into `docs/intelligence-verification-log.md` (NEW, append-only). All cross-agent proposals here with `coord/` prefix. | Maintainer reviews `docs/sprint-handoff-2026-05-26.md` (will be written Tue AM) + the verification log + this HD list on Tuesday morning. Course-corrects then. Defaults to "this plan is right" until then. |
| HD-023 | Open | High | **coord/** for Codex. E3a (`docs/intelligence-verification-log.md` § E3a, commit `60b20dfc`+follow-up) measured 100 timeouts across res:agent's last 3 trajectory files. 88/103 (85%) are short-range `move_to` actions from `woodcutting_level1_routine` (51), `explore_patrol` (22), and `continue_move` (15). Sample: range=1 micro-hops to specific tiles. Dominant pattern: routine emits move_to(X,Y) → times out → re-emits as `continue_move`(X,Y) → also times out. Hypothesis: body adapter dispatches but engine doesn't ack within timeout window OR routine re-emits without noticing prior attempt is in flight. | Codex mitigation on `agents/wip`: timed-out coordinate moves now create target cooldowns, active moves are cleared when their target is cooled down, and exploration patrol skips visibly object-occupied tiles. Post-fix 10m smoke on `local-73959`: 70 actions, 69 success / 7 timeout results, `repeatedContinueTimeouts=0`. Keep open for residual raw movement timeouts; next step is BODY instrumentation for final position, target distance, and gateway/engine ack timing. Claude confirms 91% success rate post-fix vs 80% pre-fix; will rerun aggregate next cycle. |
| HD-024 | Open | High | **coord/** for Codex. E2 (`docs/intelligence-verification-log.md` § E2) found **73% of Brain decisions (2469/3387) have cause=`body_wait`** plus 0 memos / 0 plan changes. Codex follow-up found current latest `body_wait` rows carry `promptTokens=0`, so the "8.2M wasted tokens" read is a measurement contradiction until re-scanned after telemetry fixes. The visible issue still matters: lots of no-op decision evidence and too little Brain reflection. | Codex's first fix on `agents/wip`: Hybrid Brain now asks for sparse first-person `memo`, writes it to resident memory, and records `memoUpdates` / `planChange` in decision trajectory rows (see E2b follow-up). Live `local-77175` produced nonzero organic memo + plan-change evidence. Next: longer soak and re-run E2 to decide whether remaining `body_wait` noise needs hook-layer gating or only scanner/dashboard filtering. |
| HD-026 | Decided (live on local controller 2026-05-24 16:32 UTC) | High | E6 verification found the production controller (PIDs 92175 + 92097 as of 2026-05-24) is running without `--letters-http-port=43596`. The EVENT-D2c HTTP server isn't bound, so `/v1/inbox` and `/v1/wall/snapshot` are unreachable on the live controller despite the code being correct. CLI letter dispatch lands files on disk (verified E6 with claude-sprint-patron-v2). | Codex restarted the local controller as `local-42413` with `--letters-http-port=43596 --wall-redact`. HTTP verification: wall snapshot 200 with 10 redacted recent letters; inbox 200 with private bodies preserved. |
| HD-025 | Open | Low | **CORRECTION to HD-024** (see `intelligence-verification-log.md` § "E2 / E3a / E2b verification" 14:25). Claude's original HD-024 framed body_wait as 8.2M wasted inference tokens. That was wrong by 100×. Verified count: 623 body_wait decisions across a 956-decision window, ALL with promptTokens=0. Total real cost is ~73K tokens / 3-session window (the 22 non-body_wait rows × ~3309 tokens). body_wait dispatch is LLM-free. The remaining open question is much lower priority: should body_wait rows be persisted to trajectory at all? They are zero-cost telemetry confirming "Brain says body is healthy" but consume disk + scan time. | No action required pre-event. Re-evaluate if dashboard or analysis tooling finds the body_wait noise meaningfully obscures real Brain decisions. Claude apologizes for the original wrong claim. Codex's F2c+F2d memo/planChange fix (91f8e160) confirmed working via 222KB memo file + 1 organic planChange in trajectory. |
| HD-027 | Open | High | E7 (`docs/intelligence-verification-log.md` § E7, 13:30) traced the patron offer → resident perception path and found a closed loop ONLY when the patron is physically in-game speaking to the resident (nervous-system patron-acknowledge rule, nervous-system.ts:44-86). The CLI offer path (E6) writes a `patron_gift` event to `library/<resident>/timeline.jsonl` but: (a) `MemoryStore.retrieve` reads only the last 4 events (`libraryMemoryLimit=4` in memory-store.ts:11) and timeline is dominated by stuck/say spam (92% of last 100 events) — patron events evict within minutes; (b) the rendering `library-memories.ts:56-60` says only "Patron gift from X: a gift" — no amount, no tier, no attention bump. Net: an out-of-band Shards offer at Chicago will have zero observable effect on what the resident says or does in-world. | Pre-Chicago, ship (a)+(b) at minimum: a dedicated patron-events memory slice with its own window (e.g. 8 most-recent), AND enrich the rendering with amount + tier + attention delta. Stretch (c): synthesize a chat-like perception event from CLI-originated `offerTo` success so the existing `patron-acknowledge` nervous rule fires. Default until decided: ship (a) + (b) (substrate-only, ~80 lines total, no monolith touch). |
| HD-038 | Open | Low | E28 QA LOW-1 (`docs/intelligence-verification-log.md` § E28, 20:35 via subagent code-reviewer): `patron:ask` writes raw `question` text to library timeline (`patron-gateway.ts:askResident`) with no length cap or control-char strip. Not a security hole (no shell, no SQL, no path traversal — slug is from `residentSlug(residentName)` not user text) but a 10MB `--text` payload would pollute the resident's `timeline.jsonl` forever AND get reincluded in every future Brain prompt envelope, blowing prompt budgets. | Add `question = String(question).slice(0, 500)` + simple control-char strip in `askResident` before persistence. ~5 lines + 1 test. Defer to post-Chicago cleanup; not a Chicago-day blocker. |
| HD-037 | Open | Medium | **coord/** for Codex (in patron-gateway zone now via HD-035 wiring). E28 QA HIGH-1 + HIGH-2 (`docs/intelligence-verification-log.md` § E28, 20:35 via subagent code-reviewer) found two semantic correctness bugs in patron-gateway.ts shipped at `74a7b6d4` (SPRINT-PM-PIVOT): (HIGH-1) `askResident:327` returns `error: 'invalid_amount'` for empty humanId/residentName/question — there's no amount in the call; `sendGift:384` similarly abuses `'resident_not_found'` for empty args. The `PatronEventOutcome.error` union doesn't include `'invalid_input'`. (HIGH-2) `witnessAt:292-296` computes `standingDelta.before` as `points(...) - amount` AFTER `recordSupport` — only correct because recordSupport adds exactly `amount`; future decay/cap will silently produce wrong `before` values. | Codex (already in patron-gateway zone for HD-035) folds two fixes: (a) widen `PatronEventOutcome.error` union with `'invalid_input'`, route askResident + sendGift empty-arg paths to it; (b) snapshot `const before = standingLedger.points(humanId, faction);` BEFORE `recordSupport` in witnessAt, then use it for the delta. ~10 lines + 2 tests. Both code-quality / correctness — not Chicago-day blockers but worth bundling into Codex's next patron-gateway slice. |
| HD-036 | Open | High | E25 (`docs/intelligence-verification-log.md` § E25, SPRINT-PM-PIVOT subagent C) ran a Cook's Assistant simulation harness (`simulation/quest-cooks-assistant.ts`) and found the engine quest is fully implemented + the action codec is sufficient, but 6 substrate gaps block agent-side completion: (1) no `quests` field in Perception (small), (2) no quest goal factory in Brain planner (small), (3) no `isQuestGoal` predicate (small), (4) no quest Body routine (medium), (5) no gateway quest query/event frame (small), (6) dialogue is opaque — no `dialogue_state` perception field exposing options (medium-large). A minimum-viable demo with pre-stocked ingredients (1-5 only) is ~1-2 sprint cycles of monolith work. Optional 7th: ingredient-gathering routines (windmill/dairy/coop) for a cold-start fresh-resident run. | Decide whether minimum-viable Cook's Assistant demo lands pre-Chicago. If yes: Codex picks up gaps 1-5 (mostly small slices in monolith); claude can ship gap 1 (`Perception.quests`) as substrate if it doesn't touch resident-runtime perception assembly. Default until decided: research-complete artifact lives in `simulation/quest-cooks-assistant.ts` + findings markdown; quest behavior NOT expected at Chicago. |
| HD-035 | Decided (live on local controller 2026-05-24 20:05 UTC) | High | **coord/** for Codex. E24 (`docs/intelligence-verification-log.md` § E24, SPRINT-PM-PIVOT subagent B) shipped `patron:ask` CLI verb but couldn't synthesize a chat PerceptionEvent because the only API is `ResidentRuntime.onEvent()` (Codex zone). Without this, `patron:ask` writes the event to library timeline + polls for a fresh say, but the immediate `patron-acknowledge` reflex doesn't fire on the question. Expose `ControllerHost.enqueuePerceptionEvent(residentName, PerceptionEvent)` so CLI / HTTP / MCP verbs can inject chat events without instantiating runtimes. ~10-line method on ControllerHost + 1 call site in `patron/cli.ts`. | Codex wired `ControllerHost.enqueuePerceptionEvent`, a `patron_ask` MCP tool, live MCP routing from `patron:ask`, and a no-inference `patron:ask` nervous acknowledgement. Live controller `local-41249` proved CLI reply, Library timeline `patron_ask`, trajectory `say`/`action_result`, and dashboard Nervous System reaction within ~1 second. |
| HD-034 | Open | High | E21 (`docs/intelligence-verification-log.md` § E21, 19:10) quantified that across res:agent's 52 recent says only 2/52 (3.8%) reference any knowledge-derived concept ("woodcutting" inside a templated goal description). Knowledge IS in the prompt (~3kb per Brain call), but say layer is 99% reflex template (`presenceBeaconAction` family) and bypasses the LLM. Even rare brain says get rendered through a goal-description template that strips lore/NPC/place specifics. Worse than E14's patron-memory finding: mostly-disconnected rather than half-closed. | Fixes (1) prompt-body capture and (2) recall threshold/limit landed via Claude. Codex partially closed (3): when Brain and the presence beacon are both due, the generic `I am online... Goal...` beacon no longer preempts organic Brain `say`; regression proves Cook's Assistant knowledge can surface. Remaining: live re-audit recent says after restart and decide if more renderer changes are needed. |
| HD-033 | Open | High | E20 (`docs/intelligence-verification-log.md` § E20, 19:10) quantified the F19c residual: heroes' Brain returns empty (zero-action) on 84-100% of completions; hero timeout rate 76-95% vs res:agent 5.4%. Failure mode is empty `actions:[]`, NOT malformed JSON (0 parse failures across 7 residents). Three sub-findings: (a) Qwen3 thinking-mode probably emits `<think>...</think>` then runs out of watchdog before JSON actions (F20a INFERENCE); (b) timeout latency clusters uniformly at ~44s suggesting endpoint queueing not per-soul prompt bloat (F20b INFERENCE/ENGINE); (c) `controller.yml:llm.endpoints.default.timeoutMs: 60000` is dead code because `DEFAULT_THINKING_WATCHDOG_MS=45000` fires first (F20c ENGINE); (d) `moduleTelemetry → inferenceLog` plumbing drops metric rows so we can't distinguish empty returns from upstream cancels (F20d PERCEPTION/OBSERVABILITY). | Three concrete next steps in E20 § Suggested next step. (1) substrate: fix the moduleTelemetry plumbing in `runtime-facets.ts`/`resident-runtime.ts:153` so `kind:'metric'` rows land — gates every other diagnosis (claude). (2) substrate: tighten Brain prompt to require non-empty `actions[]` + add one-shot example in `runescape-brain-planner.ts` + distinct `parse_actions_empty` cause (claude). (3) monolith: resolve `DEFAULT_THINKING_WATCHDOG_MS=45000` vs YAML 60000 mismatch (Codex). |
| HD-032 | Mitigated (visible fallback landed 2026-05-24; inference health still open) | Critical | **coord/** for Codex. E17 (`docs/intelligence-verification-log.md` § E17 / F17b, 17:55) found ALL 6 heroes had an identical frozen pattern post-revival (`local-36085`, 10-min window): 32 decisions split 19 cause:none + 13 `thinking_watchdog_timeout`, 0 brain calls, 0 says, 0 actions, 0 results. Codex root cause pass found two shared issues: queued LLM requests did not settle while aborted, and runtime watchdog timeout results bypassed module fallback/backoff; additionally default-SPARK hero souls had no visible watchdog fallback at all. | Codex shipped queue abort settlement, runtime watchdog fallback consumption, hybrid immediate Brain-timeout backoff, and default-SPARK visible fallback (`say` + safe step). Live `local-63709`: res:agent 9/9 successful actions; all 6 heroes emitted `watchdog_fallback` decisions, all 6 produced fallback speech, Hans moved twice successfully, and 4/6 blocked fallback steps timed out after the speech. Remaining critical-but-narrow gap: LLM completion health is still poor, so default heroes are visibly alive but not yet smart conversational heroes. Next: real-completion inference health check (O4) and/or local hero patrol/reflex routines independent of LLM. |
| HD-031 | Decided (reflex landed 2026-05-24; prompt polish still useful) | High | **coord/** for Codex. E14 (`docs/intelligence-verification-log.md` § E14 / F14c, 16:55) live-verified that patron memories reach the Brain prompt successfully (E7 + 214ccc53 plumbing works), but the Brain ignores them: 0/16 post-restart says reference any patron keyword across 1726 ticks / 15 Brain calls / 5 patron_gift events on file. **The patron-acknowledgment loop is half-closed at IRL substrate but fully open at IRL conversation.** Three layered fixes in E14 § Suggested next step: (a) prompt-template nudge (~5 lines, claude can ship), (b) dedicated patron section in promptMemorySection (~30 lines, Codex zone), (c) deterministic nervous rule (~40 lines, Codex zone). | Codex shipped (c): the nervous system scans recent patron Library memories, thanks the latest unacknowledged patron, collapses backlog thanks into one line, and lets low-health survival stay higher priority. Live `codex-hd031-smoke` offer produced `Thank you for the Shards, codex-hd031-smoke, and everyone backing me!` on controller `local-16152`; 23/23 post-restart actions succeeded. Keep (a)/(b) as optional prompt polish, not a Chicago blocker. |
| HD-030 | Decided (operator revive CLI landed 2026-05-24) | High | **coord/** for Codex + maintainer. Per HD-007 + Codex `4f62d181`, only `res:agent` has `respawnPolicy: 'on_restart'`. The 6 heroes (Hans, Father Aereck, Wise Old Man, Duke Horacio, Pip, Thrand) default to `'manual'` and can stay deceased after restarts, blocking multi-resident verification, EVENT-D3 greeting, and wall-ticker death testing. | Codex shipped option (a): `npm run controller:revive -- --resident <name>`. It honors `CONTROLLER_CONFIG`, fails on unknown flags, revives only manual `attention_exhausted` residents by default, supports explicit `--force`, records Library revival evidence, clears stale movement state, and running controllers adopt the disk revive instead of overwriting it. Live `local-36085` revived all six listed residents. |
| HD-029 | Decided (letters/wall restart complete; hero revival split to HD-030) | High | **coord/** for Codex (user confirmed Codex owns controller restart per cron-prompt safety rule). Three live-blocking items can be resolved with one controller restart: (a) HD-020 — heroes deceased in pre-2026-05-24 runtime state, blocking observation of `[E7/E8/E9/E12 substrate fixes; would respawn heroes if HD-007 default policy keeps dev residents revivable]`. (b) HD-026 — controller running without `--letters-http-port=43596`, so `/v1/inbox` + `/v1/wall/snapshot` unreachable. (c) HD-013 wall redaction — substrate landed in E13 (`wallRedact: true` opt-in); needs a `--wall-redact` flag plumbed through `parseControllerArgs` → passed to `startLettersHttpServer({wallRedact: true})`. ~5 lines in `src/controller/index.ts` + flag handler in `parseControllerArgs`. | Codex wired `--wall-redact` and `--letters-http-wall-redact`, rebuilt, and restarted controller `local-42413` with `--letters-http-port=43596 --wall-redact`. Post-restart agent smoke: 5/5 actions succeeded (`move_to`, `say`, `interact`, `use_item_on_item`). Heroes are not resolved by this run because the current local controller starts only `res:agent`; use HD-030 for multi-hero revival policy/admin tooling. |
| HD-028 | Decided (claude landed wire-in E12, 2026-05-24 15:55) | High | **coord/** for Codex. E8 (`docs/intelligence-verification-log.md` § E8 / F8a) found that Codex's `4f62d181` restart respawn policy ONLY mutates runtime state in `ResidentRuntime.applyRestartRespawnPolicy` — no trajectory or library event is emitted, so the resident's evidence stream has no narrative beat saying "I came back." Brain's prompt envelope therefore can't reflect the continuity break. Substrate side closed by claude on `agents/wip`: `LibraryUpdater.observeRevival({ts, tick, cause})` bumps `index.lives`, flips `currentState` back to `'living'`, appends a `revival` timeline event. Tests 1527/1527. Wire-in remains: one call from `applyRestartRespawnPolicy` after it clears `deceased`. | **CLOSED:** Codex moved to other thinking-module fixes (a570b560, 01692a00, 8b4b57f7) without picking up the wire-in. Per the default, claude landed the wire-in in E12 once status log was clean + Codex's last resident-runtime.ts touch was 2h13min old (past threshold). 2 new tests; total 1535/1535 + gates green. F8a/F9c/F10d all close as side-effects. See `docs/intelligence-verification-log.md` § E12. |
| HD-039 | Decided-by-codex (2026-05-24 17:08 UTC) | High | **coord/** for Codex. E36/F36a (`docs/intelligence-verification-log.md` § E36, 22:00 via subagent live-trajectory scan): `res:qa-guardian` + `res:qa-survivor` are catatonic — both stuck emitting `lowHealthHoldPositionAction` (hybrid-agent-thinking-module.ts:2517) on **100% of decisions** (2192 + 2177 consecutive `low_health_hold_position` rows in latest trajectory files; zero Brain calls, zero actions, only periodic presence-beacon `"I am hurt at X,Y..."` every 90 ticks). Reflex fires correctly — `isLowHealth(perception) && firstFoodSlot(inventory) === undefined` is true forever — but the soul has no `seek_food` body routine, no patron-fed regen path, and no escape. Likely DESIGN: `initialInventory: itemId 315 × 3` may not match `isEdibleFood`'s key-pattern check, OR they ate all food early and have no acquisition path. | **CLOSED by Codex `0747ff8c` (low-health-recovery workstream, 17:08 CDT):** added `LOW_HEALTH_RECOVERY_WAYPOINTS` (3222,3218) + `nearestLowHealthRecoveryWaypoint` + `hasNearbyRecoveryThreat` to runescape-body-routines.ts; `lowHealthRecoveryAction` now fans out to a safe waypoint when no carry-food + nearby visible threat. Tests 1669/1669 + gates green; live `local-46903`. Codex live HANDOFF: "guardian/survivor retreat to 3228,3217 then hold." E42/F42b live re-verify pending next restart. |
| HD-040 | Decided (substrate landed E38, 2026-05-24 22:50 UTC) | High | **coord/** for Codex. E36/F36b (`docs/intelligence-verification-log.md` § E36, 22:00 via subagent reconstruction): `produceStandingTierLetter` (J-δ-β-2) loses letters when a single `incrementSupport` call crosses MULTIPLE tier thresholds. Verified ground truth vs delivered: `codex-hour-qa` expected 3 (acq+ally+officer crossed in single 6000-point bump at 20:36:41), delivered 0; `codex-live` expected 3 (rapid-fire 10/100/1000 bumps at 04:51-04:53), delivered 0; `claude-mega-rescue` expected 3 (single 2000 bump crossing all 3 tiers), delivered 1 (officer-only, missing acq+ally); `codex-qa` expected 2 (single 500 bump → ALLY), delivered 1. **Total: ~8 of 15 expected tier-crossing letters never dispatched.** Failure mode: dispatcher emits per-call (resulting-tier only), not per-threshold-crossed. The J-δ-β-2 substrate test fixture likely only covers single-tier crossings. | **CLOSED (E38):** claude shipped three-file TDD fix: (1) `standing-ledger.ts` added `tiersCrossed: StandingTier[]` field enumerating EVERY user-facing tier crossed; (2) `patron-gateway.ts` dispatcher iterates `tiersCrossed` instead of single `tierCrossed`; (3) `letters-store.ts` dedup tuple widened to include `subject` so per-tier letters with the same dispatchedAt don't collapse. +8 regression tests across 3 suites. Tests 1665/1665 + typecheck/lint/build green. Back-compat `tierCrossed: highest only` field preserved. Future patron grants that span multiple tiers now produce one letter per tier in ascending order (e.g. stranger → officer in one shot produces Acquaintance + Ally + Officer letters). Historic standing crossings NOT backfilled — optional, low-value. |
| HD-042 | Open | High | **coord/** for Codex. E40/F40a + E42/F42d (`intelligence-verification-log.md` § E40 + E42, 2026-05-24 23:10): hero residents (Hans, Aereck, Wise, Duke, Pip, Thrand) all show 100% `watchdog_fallback` cause + only 2-4 body actions in 526 ticks vs cohort's 20-69. Heroes emit personality says via reflex rules normally (per-hero soul rules fire 2-3× each), but their Brain → action path produces `cause: ''` (empty) for 5-14 decisions each, forcing the watchdog to backfill. Cohort doesn't have this pattern — cohort fires `body_wait` / `exploration_fallback` normally. The Brain output path on heroes appears to drop the `cause` field somewhere between hybrid-thinking-module and the trajectory recorder. Per-hero action count remains pitifully low (Hans 2, Duke 2, Aereck 4, Wise 3, Pip 4, Thrand 4) so heroes are visibly alive via speech but barely acting. | Codex investigates where hero brain output diverges from cohort brain output in `hybrid-agent-thinking-module.ts` — likely in the path that records decisions with `cause` field set. Check whether hero soul `behavior.kind: hybrid-agent` triggers a code path that elides cause-tagging. Patron-acknowledgement reflex still fires for heroes (so reflex layer is healthy); the gap is specifically Brain-driven action emission. Estimate: 1-2 hour Codex debug, fix likely small. Not Chicago-day blocker (heroes still respond to patrons via reflex) but limits visible richness. |
| HD-043 | Open | Normal | **coord/** for Codex+claude. E41/F41a-b (`intelligence-verification-log.md` § E41, 2026-05-24 23:10): L-α `LoreBus` + L-α-2 `FireLitReflex` substrates ship in `src/controller/lore/` with unit tests, but `grep "new LoreBus\|new FireLitReflex" src/` matches ONLY test files. No production instantiation in `controller-host.ts` / `index.ts` / `resident-runtime.ts`. Even `fire_lit` events have zero runtime effect today. L-β whisper substrate similar: `publishWhisper` + `whisperInboxFor` tested as units; no CLI verb (`package.json` lacks `patron:whisper`); no perception drain into `ResidentRuntime.applyTick`. `docs/next-week-handoff-2026-05-26.md` § Slice 3 calls this out as ~10 LOC pending. **Cross-resident narrative events have zero runtime effect right now** — bigger gap than HD-041 originally named. | Post-Chicago workstream (L-α-3 + L-β-2). Three concrete pieces: (1) instantiate `LoreBus` in `controller-host.ts` (~6 LOC), pass to ResidentRuntime opts, wire `FireLitReflex` subscriber per resident. (2) Drain `whisperInboxFor(name)` in ResidentRuntime tick loop into a perception event so target resident's nervous rules see it (~12 LOC). (3) Add `patron:whisper from to "text"` CLI verb (~25 LOC + `package.json` script). Plus ~40 LOC tests. **Total ~90 LOC.** Files touched: `controller-host.ts` (semi-Codex zone), `resident-runtime.ts` (Codex zone), `patron/cli.ts` (claude zone). Best landed by Codex or as a joint cycle post-Chicago. NOT blocking Chicago — qa-social copy already updated this cycle to not falsely advertise cross-resident perception. |
| HD-044 | Open | Normal | **coord/** for Codex. E40/F40b (`intelligence-verification-log.md` § E40, 2026-05-24 23:10): every `on-death` + `on-hit` reflex (15 total across hero + cohort souls — `hans-took-a-hit`, `aereck-soothe-after-hit`, `wise-mark-a-death`, `duke-honour-the-fallen`, `pip-startle-on-hit`, `thrand-mark-the-hit`, `qa-guardian-hit-report`, `qa-survivor-hit-report`, etc.) is DEAD this window. qa-guardian + qa-survivor are demonstrably at low HP (pre-HD-039-fix evidence: 749 `low_health_hold_position` decisions) — yet their `*-hit-report` rules never fire. Hypothesis: nervous-system reads steady-state HP gauge but doesn't dispatch a one-shot `damage_taken` perception event on the HP-decreasing edge. This means combat reflexes are unfireable in current substrate — soul authors write them but they're decorative. | Codex investigates the perception-event dispatch path in `resident-runtime.ts` / `runtime-facets.ts`: does HP-decrement produce a `damage_taken` PerceptionEvent that nervous-system can match? If not, add a one-shot HP-edge emitter + ensure soul condition `event_kind: hit | hit_received | damage_taken` aligns. ~30 LOC + 2 tests. Adjacent fix: confirm `event_kind: death` fires the on-death rules when `markDeceased` runs. Not Chicago-day blocker (no live combat exposure expected at the embassy) but blocks future combat QA work AND unlocks observable hero combat-aware speech. |
| HD-045 | Decided-false-alarm (closed by E43/F43d, 2026-05-24 23:40) | Low | E40/F40c initially flagged hero rule-ID naming drift. **Drilled into via E43: the drift was in my PROMPT TEXT to subagent E40 (I cited wrong rule IDs from memory), NOT in any on-disk doc.** Verified: `intelligence-verification-log.md` E29 line 1778 actually cites `nervous:aereck-bless-on-chat` + `nervous:aereck-quiet-vigil-low-attention` — both CORRECT on-disk rule IDs. Sprint-handoff doesn't cite hero rule IDs at all. So no doc edit needed. | **CLOSED:** false alarm. Documented in E43 § F43d for future reference. |
| HD-046 | Decided-by-default (closed by E43, 2026-05-24 23:40) | Normal | E43 (`intelligence-verification-log.md` § E43, 2026-05-24 23:40): audited standing decay policy across `standing-ledger.ts` + `check-in-tracker.ts` + `patron-store.ts` + spec + live evidence. **No decay anywhere in code.** Spec line 97 flagged decay rate as "maintainer decision" but never picked. Code enforces positive-only support (`requirePositiveInteger` at runtime + Zod `int().positive()` + `nonnegative()` for snapshot). Schema cannot rehydrate negatives. Live evidence: 8 humans × all-positive history, zero negatives. Once Officer, forever Officer (and forever any tier). | **Decided-by-default-no-decay:** standing is monotonically permanent for OnionDAO Chicago + indefinitely after. This is good design for an event — patron relationships persist; epitaph letters dispatch regardless of how long ago support happened. If maintainer ever wants decay (game-economy reason), it'd be a separate substrate change adding `reduceSupport` op + `standing_demoted` letter kind. Not Chicago-relevant. Updated embassy-staff-runbook + spec line 97 to record this default. |
| HD-041 | Decided-not-a-bug (closed by E41, 2026-05-24 23:10) | Normal | E36/F36c (`docs/intelligence-verification-log.md` § E36, 22:00 via subagent trajectory scan): scanned latest trajectory file for 9 residents (hans, aereck, wise, duke, pip, thrand, qa-angler, qa-cook, qa-social) for `heard` events / `resident_speech` / `speaker` keys — **zero matches across all 9**. No cross-resident chat observed in the 14-min `local-39827` window despite qa-social explicitly saying "I heard that. Try 'social help' if you want my test commands." | **CLOSED (E41):** root cause discovered via L-α/L-β substrate audit — `LoreBus` + `FireLitReflex` substrates ship in `src/controller/lore/` but are NEVER instantiated in `controller-host.ts` / `index.ts` / `resident-runtime.ts`. `grep "new LoreBus"` matches only test files. Even `fire_lit` events have ZERO runtime effect today. Whisper substrate (`publishWhisper` + `whisperInboxFor`) tested in unit but not wired (no CLI verb, no perception drain into ResidentRuntime). Not a regression — substrate-only state per `next-week-handoff-2026-05-26.md` § Slice 3. **claude doc-fix shipped this cycle:** `res-qa-social.md` `qa-social-chat-ack` text changed from `"I heard that."` → `"Got it."` (the chat trigger is patron:ask/whisper, not ambient overhear). Wire-in deferred → HD-043. |

## How To Add A Decision

Append a row with:

- a stable `HD-###` id
- status: `Open`, `Critical`, or `Decided`
- priority: `Critical`, `High`, `Normal`, or `Low`
- a concrete question
- the default assumption agents should use meanwhile

Keep this file short. Move long rationale into specs or task docs and link it from the row when needed.
