# QA Wall Truth Recheck

Packet: `qa-20260603-1831-wall-truth-recheck`
Scope: READ-ONLY live verification
Related proposed issue: `QA-20260603-089`
Severity: P1
Area: public wall / Library / graveyard truth

## Finding

The public wall truth gap from the earlier handoff is still live on the current shared stack.

- `http://127.0.0.1:5174/debug/wall` still shows repeated `[Broadcast] On the passing of res:loop-check` cards.
- The same wall page still mixes in stale residents such as `res:wf-verify-born`, `res:mother-anvil`, `res:severn-vesta`, `res:the-hush`, and other offline/non-cohort names.
- `http://127.0.0.1:5174/debug/graveyard` simultaneously says `No residents have died yet.`
- The live controller runtime is healthy enough to disprove a general stack outage: `npm run controller:status` reports `HEALTH: ok`, `residents=25`, and `10` currently active/acting residents; `npm run controller:smoke -- --observe-seconds 15 --allow-recent-visible` returned `9 OK / 1 WARN`.

This remains a human-visible truth bug, not a runtime outage.

## Current Evidence

Verification time: `2026-06-03 18:24-18:31 CDT`

Runtime truth:

- `screen -ls`: expected sessions present: `nullcity-infra`, `nullcity-game`, `nullcity-controller`, `nullcity-dashboard-server`, `nullcity-dashboard-web`
- `lsof -nP -iTCP -sTCP:LISTEN`: expected listeners present on `43591`, `43592`, `43594`, `43595`, `43596`, `43610`, `43611`, `5174`, `8787`
- `npm run controller:status`:
  - `HEALTH: ok — probe ok (17553ms)`
  - `residents=25 erroring=0`
  - active/acting now: `res:hans`, `res:qa-cook`, `res:qa-guardian`, `res:qa-scout`, `res:qa-social`, `res:qa-survivor`, `res:qa-trader`, `res:qa-woodcutter`
  - stuck but attached: `res:agent`, `res:qa-banker`
- `npm run controller:smoke -- --observe-seconds 15 --allow-recent-visible`:
  - `OK`: `res:agent`, `res:hans`, `res:qa-woodcutter`, `res:qa-cook`, `res:qa-survivor`, `res:qa-trader`, `res:qa-banker`, `res:qa-social`, `res:qa-scout`
  - `WARN`: `res:qa-guardian` with `effect_timeouts:1`

Live page truth was verified in the in-app browser because shell loopback probes are blocked in this sandbox even when listeners are present.

Observed public wall page:

- Route: `http://127.0.0.1:5174/debug/wall`
- Title: `Null City Embassy — Wall`
- Visible stale/cohort-mismatched resident roster includes `res:mother-anvil`, `res:severn-vesta`, `res:the-hush`, `res:wf-verify-born`, `res:wren-calix`, `res:duke-horacio`, `res:father-aereck`, `res:pip`, `res:thrand`, `res:wise-old-man`
- Duplicate broadcast count visible on page during this run: `8`
- Repeated visible subject: `[Broadcast] On the passing of res:loop-check`
- Page footer: `0 deaths today as of 06:31 PM`

Observed graveyard page:

- Route: `http://127.0.0.1:5174/debug/graveyard`
- Title: `Null City — Graveyard`
- Visible body: `No residents have died yet.`

Observed Library page:

- Route: `http://127.0.0.1:5174/debug/library`
- Title: `Null City — Library of Souls`
- Still shows stale/non-cohort residents including `res:wf-verify-born`
- Shows `res:restart-test` as `reborn` and living, which reinforces that the wall's repeated `res:loop-check` death broadcast is not grounded by the current graveyard page

## Assessment

The earlier wall handoff was not a transient cache artifact. The current live dashboard still presents death/broadcast history that conflicts with current graveyard truth and the active controller cohort.

The duplicate count changed from the earlier API handoff (`12`) to the currently visible wall page (`8`), but the underlying defect remains: public wall output is still not aligned with current graveyard/Library truth.

## Next Action

When `docs/issue-register.md` is safe to edit, keep or add the canonical row for `QA-20260603-089` and route it to the dashboard/server public wall path.

Likely fix area:

- `rs6-nullcity-residents-dashboard/packages/server/src/event-public.ts`

Expected regression coverage:

- dedupe broadcast letters across recipients on the wall/public snapshot path
- suppress death/passing broadcasts that are not supported by current graveyard/Library public truth
- verify stale/non-cohort residents do not dominate the public wall roster when the live controller cohort is only `10` active residents
